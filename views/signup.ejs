<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body data-page="signup">

    <div class="parent-container parent-signup">
        <div class="form-wrapper">
            <div class="form-container">
                <h3 class="text-center mb-4 primary-text heading-text">Signup</h3>

                <form id="signupForm" action="/signup" method="post">
                    <div class="mb-3">
                        <label class="form-label primary-text" for="username">Username</label>
                        <input class="form-control primary-text black-text" id="signup-username" name="username" type="text" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label primary-text" for="password">Password</label>
                        <input class="form-control" id="signup-password" name="password" type="password" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label primary-text" for="confirmedPassword">Confirm Password</label>
                        <input class="form-control" id="signup-confirmedPassword" name="confirmedPassword" type="password" required>
                    </div>

                    <p class="align-right">
                        <a class="primary-small-text font-italic" href="#" onclick="generateUsername()">Generate Username?</a>
                    </p>

                    <button class="primary-text white-text btn btn-primary w-100" type="submit">Signup</button>

                    <div class="divider">
                        <span>or</span>
                    </div>

                    <p class="primary-small-text align-center">Already Have an Account? 
                        <a class="primary-small-text font-bold-italic" href="/login">Login</a>
                    </p>

                    <% if (error) { %>
                        <p class="primary-small-text red-icon align-center"><%= error %></p>
                    <% } %>
                </form>
            </div>
        </div>

        <div class="visual-container">
            <img class="align-center" src="/images/note.png" alt="Note Image">

            <h3 class="primary-text heading-text align-center">Bienvenue Folk!</h3>
            <p class="primary-text align-center">Comment ca va? Ta familla ca va? Too many tasks for a person to manage is kind of cruel. Hope you can find someone to share your tasks and responsibilities. A bientot!</p>
        </div>
    </div>

    <script src="/js/script.js"></script>
</body>